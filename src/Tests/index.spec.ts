// eslint-disable-next-line no-undef
describe('Setup Jest + TS', () => {
  // eslint-disable-next-line no-undef
  test('criando asserção', () => {
    // eslint-disable-next-line no-undef
    expect(1 + 1).toBe(2)
  })
})
